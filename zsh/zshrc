   
for zsh_source in $HOME/.zsh/configs/**/*.zsh; do
  source $zsh_source
done


export ZSH=~/.oh-my-zsh

ZSH_THEME="robbyrussell"

 plugins=(git)

source $ZSH/oh-my-zsh.sh

_not_inside_tmux() { [[ -z "$TMUX" ]] }

ensure_tmux_is_running() {
  if _not_inside_tmux; then
   ~/tat.sh
  fi
}

ensure_tmux_is_running

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# General ZSH configurations

export EDITOR="vim"

sz() { source ~/.zshrc }
bindkey -v

export PATH=$PATH:/home/eduardoborsa/programs/clang_Build/bin/

BASE16_SHELL=$HOME/.config/base16-shell/
[ -n "$PS1" ] && [ -s $BASE16_SHELL/profile_helper.sh ] && eval "$($BASE16_SHELL/profile_helper.sh)"

export PATH=$PATH:/home/eduardoborsa/programs/clang_Build/bin/
base16_solarized-dark 
export PATH=$PATH:~/WebstormProjects/Pluralsight/Initial/Mine/node_modules/
export PATH=$PATH:~/niceScripts/
export PATH=$PATH:~/programs/node-v8.2.1-linux-x64/bin/
export PATH=$PATH:~/programs/clion-2017.2.2/bin/
export PATH=$PATH:/opt/mssql-tools/bin
export ANDROID_HOME=$HOME/programs/android-studio/bin/
