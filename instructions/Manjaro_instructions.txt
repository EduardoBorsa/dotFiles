* BASIC PACKAGES TO START
sudo pacman -Syu --noconfirm i3 &&
sudo pacman -Syu --noconfirm dmenu &&
sudo pacman -Syu --noconfirm neovim &&
sudo pacman -Syu --noconfirm git &&
sudo pacman -Syu --noconfirm jq &&
sudo pacman -Syu --noconfirm tk &&
sudo pacman -Syu --noconfirm xorg-xbacklight &&
sudo pacman -Syu --noconfirm xorg-xmodmap &&
sudo pacman -Syu --noconfirm fzf &&
sudo pacman -Syu --noconfirm the_silver_searcher &&
sudo pacman -Syu --noconfirm python-pip &&
sudo pacman -Syu --noconfirm base-devel devtools &&
sudo pacman -Syu --noconfirm yaourt &&
sudo pacman -Syu --noconfirm ctags &&
sudo pacman -Syu --noconfirm playerctl &&
sudo pacman -Syu --noconfirm feh &&
sudo pacman -Syu --noconfirm thunar &&
sudo pacman -Syu --noconfirm arandr &&
sudo pacman -Syu --noconfirm xorg-xprop &&
sudo pacman -Syu --noconfirm arc-gtk-theme &&
sudo pacman -Syu --noconfirm moka-icon-theme &&
sudo pacman -Syu --noconfirm rofi &&
sudo pacman -Syu --noconfirm gtk2 &&
sudo pacman -Syu --noconfirm i3blocks &&
sudo pacman -Syu --noconfirm pavucontrol &&
sudo pacman -Syu --noconfirm postgresql &&
sudo pacman -Syu --noconfirm tmux &&
sudo pacman -Syu --noconfirm ttf-font-awesome &&
sudo pacman -Syu --noconfirm lxappearance &&
sudo pacman -Syu --noconfirm arc-gtk-theme

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
chsh -s /usr/bin/zsh



commands for vim:
1) curl -fLo ~/.local/share/nvim/site/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
1) mkdir .config/nvim/
2) ln -s ~/programs/dotFiles/nvim/init.vim .config/nvim/init.vim
3) ln -s ~/programs/dotFiles/nvim/mappings.vim .config/nvim/mappings.vim
3) ln -s ~/programs/dotFiles/nvim/functions.vim .config/nvim/functions.vim
4) ln -s ~/programs/dotFiles/nvim/general_vim_config.vim .config/nvim/general_vim_config.vim
4) ln -s ~/programs/dotFiles/nvim/vim_plug_plugins.vim .config/nvim/vim_plug_plugins.vim
8) sudo pip3 install neovim

commands for zsh:
1) rm -rf .zshrc
2) ln -s ~/programs/dotFiles/zsh/zshrc .zshrc

commands for tmux:
1) ln -s ~/programs/dotFiles/tmux/tmux.conf .tmux.conf

commands for i3:
1) rm -rf .config/i3/config
2) ln -s ~/programs/dotFiles/i3/config .config/i3/config

install collors:
git clone https://github.com/chriskempson/base16-shell.git ~/.config/base16-shell

install google chrome
9) git
10) cd ~/programs
    git clone https://aur.archlinux.org/google-chrome.git
    cd google-chrome
    makepkg -si
    # install .tar.gz
    sudo pacman -U google-chrome-6

HOW?
11) Apply sanf fonts in every gtkfile in home and .config

install ruby:
gpg --keyserver hkp://keys.gnupg.net --recv-keys 409B6B1796C275462A1703113804BB82D39DC0E3 7D2BAF1CF37B13E2069D6956105BD0E739499BDB
curl -sSL https://get.rvm.io | bash -s stable
source ~/.rvm/scripts/rvm
rvm requirements
rvm install 2.5.1
rvm use 2.5.1 --default
gem install rails -V --no-ri --no-rdoc
gem install bundler -V --no-ri --no-rdoc
gem install rufo

CONFIGURE POSTGRESQL:
sudo -i -u postgres
initdb --locale en_US.UTF-8 -E UTF8 -D '/var/lib/postgres/data'
sudo systemctl start postgresql
sudo systemctl enable postgresql

To get the San francisco fonts go to https://github.com/smith-neil/YosemiteSanFranciscoFont download
and put all the tff fonts in .local/share/fonts


--------
makepkg -si
s resolve dependencia
i instala
pacman -U pacote.pkg.tar.xz
